@model Demo.AspNetCore.Htmx.Models.Category

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Category</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CategoryName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CategoryName)
        </dd>
    </dl>
    
    <form asp-antiforgery="true" hx-target="#mainId">
        <button type="button" class="btn btn-outline-secondary" hx-get="/Category/Index">
            Back to List
        </button>
        <button class="btn btn-danger" hx-post="/Category/Delete/@Model.CategoryId">
            Delete
        </button>
    </form>
</div>
